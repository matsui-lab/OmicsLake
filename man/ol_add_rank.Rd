% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregation.R
\name{ol_add_rank}
\alias{ol_add_rank}
\title{Add ranking column to table}
\usage{
ol_add_rank(
  table,
  rank_by,
  partition_by = NULL,
  method = "row_number",
  descending = TRUE,
  as_column = "rank",
  project = getOption("ol.project"),
  collect = TRUE
)
}
\arguments{
\item{table}{Character string, name of the table}

\item{rank_by}{Character string, column name to rank by}

\item{partition_by}{Character vector of columns to partition by (default:
NULL)}

\item{method}{Character, ranking method: "row_number", "rank", or
"dense_rank" (default: "row_number")}

\item{descending}{Logical; if TRUE, rank in descending order (default: TRUE)}

\item{as_column}{Character, name of the ranking column to add (default:
"rank")}

\item{project}{Project name (default: current project)}

\item{collect}{Logical; if TRUE returns data.frame, if FALSE returns lazy
dplyr table}
}
\value{
Original table with added ranking column
}
\description{
Add a ranking column using window functions (ROW_NUMBER, RANK, DENSE_RANK).
}
\examples{
if (FALSE) {
    ol_add_rank("genes", rank_by = "expression", method = "dense_rank")

    ol_add_rank("genes",
        rank_by = "expression",
        partition_by = "sample",
        as_column = "expression_rank"
    )
}
}
