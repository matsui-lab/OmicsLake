% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{ol_plot_lineage}
\alias{ol_plot_lineage}
\title{Visualize dependency lineage as a graph}
\usage{
ol_plot_lineage(
  name,
  direction = c("upstream", "downstream", "both"),
  layout = c("tree", "sugiyama", "circle", "auto"),
  max_depth = 10,
  project = getOption("ol.project"),
  vertex.size = 20,
  vertex.label.cex = 0.8,
  edge.arrow.size = 0.5,
  main = NULL,
  ...
)
}
\arguments{
\item{name}{Name of the table or object to visualize}

\item{direction}{Direction to traverse: "upstream" (dependencies),
"downstream" (dependents), or "both"}

\item{layout}{Graph layout algorithm: "tree", "sugiyama" (hierarchical),
"circle", or "auto"}

\item{max_depth}{Maximum depth to traverse (default: 10)}

\item{project}{Project name}

\item{vertex.size}{Size of graph nodes (default: 20)}

\item{vertex.label.cex}{Size of node labels (default: 0.8)}

\item{edge.arrow.size}{Size of edge arrows (default: 0.5)}

\item{main}{Plot title (auto-generated if NULL)}

\item{...}{Additional arguments passed to plot.igraph()}
}
\value{
An igraph object (invisibly)
}
\description{
Creates a visualization of the dependency relationships between tables and
objects in the OmicsLake project. Requires the igraph package to be
installed.
}
\examples{
if (requireNamespace("igraph", quietly = TRUE)) {
    ol_init("ex_plot_lineage", root = tempfile())
    ol_write("raw", data.frame(x = 1:3))
    ol_write("f", data.frame(x = 1:2), depends_on = "raw")
    ol_plot_lineage("f")
}
}
