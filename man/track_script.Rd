% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/observe.R
\name{track_script}
\alias{track_script}
\title{Track an existing analysis script with one function call}
\usage{
track_script(
  path,
  project = NULL,
  prefix = "file:",
  snapshot = NULL,
  track_functions = NULL,
  save_result = FALSE,
  result_name = NULL,
  result_depends_on = c("writes", "reads", "both", "none"),
  store_observation = FALSE,
  observation_name = NULL,
  observation_depends_on = c("writes", "reads", "both", "none"),
  chdir = FALSE,
  echo = FALSE,
  source_args = list(),
  ...
)
}
\arguments{
\item{path}{Script path passed to `source()`}

\item{project}{Optional project name. If set, calls `use_lake(project, ...)`.}

\item{prefix}{Prefix for file-based node names in the lake}

\item{snapshot}{Optional snapshot label created after successful execution}

\item{track_functions}{Character vector forwarded to `observe()`}

\item{save_result}{If TRUE, store the script return value in the Lake}

\item{result_name}{Optional target name when `save_result = TRUE`}

\item{result_depends_on}{Dependencies used for saved result:
`"writes"`, `"reads"`, `"both"`, or `"none"`}

\item{store_observation}{If TRUE, store observed reads/writes/lineage as an
object}

\item{observation_name}{Optional target name when `store_observation = TRUE`}

\item{observation_depends_on}{Dependencies used for observation record:
`"writes"`, `"reads"`, `"both"`, or `"none"`}

\item{chdir}{Passed to `source()`}

\item{echo}{Passed to `source()`}

\item{source_args}{Named list of additional arguments passed to `source()`.
Reserved names (`file`, `local`, `chdir`, `echo`) are not allowed.}

\item{...}{Additional arguments passed to `use_lake()` when `project` is
provided}
}
\value{
The return value of the sourced script (`source(...)$value`)
}
\description{
Sources a script with `local = TRUE` inside observation mode so common
unqualified I/O calls in the script can be intercepted.
}
\examples{
if (FALSE) {
track_script("analysis_pipeline.R", project = "rna_seq_project", snapshot =
"run_2026_02_21")
}
}
