% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrap.R
\name{link}
\alias{link}
\title{Create an explicit dependency link}
\usage{
link(from, to, lake = NULL, relationship = "linked")
}
\arguments{
\item{from}{Source node name (parent)}

\item{to}{Target node name (child)}

\item{lake}{Lake instance (uses default if NULL)}

\item{relationship}{Type of relationship (default: "linked")}
}
\value{
Invisible TRUE on success
}
\description{
Manually creates a dependency relationship between two nodes in
the lineage graph.
}
\examples{
if (FALSE) {
    lake <- Lake$new("project")

    # Store some data
    lake$put("source_data", df1)
    lake$put("derived_data", df2)

    # Manually link them
    link("source_data", "derived_data", lake)

    # Now lineage shows the connection
    lake$tree("derived_data")
}
}
