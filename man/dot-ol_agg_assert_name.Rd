% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregation.R
\name{.ol_agg_assert_name}
\alias{.ol_agg_assert_name}
\title{Aggregate data with multiple statistics}
\usage{
.ol_agg_assert_name(x, arg)
}
\arguments{
\item{table}{Character string, name of the table to aggregate}

\item{group_by}{Character vector of column names to group by (default: NULL
for overall aggregates)}

\item{...}{Named arguments specifying aggregates. Each argument should be a
list with
'func' (aggregate function name) and 'col' (column name). The argument name
becomes
  the output column name.}

\item{project}{Project name (default: current project)}

\item{collect}{Logical; if TRUE returns data.frame, if FALSE returns lazy
dplyr table}
}
\value{
Aggregated results as data.frame (if collect=TRUE) or lazy table (if
collect=FALSE)
}
\description{
Calculate multiple aggregate statistics, optionally grouped by columns.
Supports common aggregates: count, sum, avg, min, max, stddev, median, etc.
}
\examples{
if (FALSE) {
    ol_aggregate("genes",
        mean_expr = list(func = "avg", col = "expression"),
        sd_expr = list(func = "stddev", col = "expression")
    )

    ol_aggregate("genes",
        group_by = "sample",
        mean_expr = list(func = "avg", col = "expression"),
        count = list(func = "count", col = "*")
    )
}
}
