% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{ol_query}
\alias{ol_query}
\title{Execute a custom SQL query on the OmicsLake database}
\usage{
ol_query(sql, project = getOption("ol.project"), collect = TRUE, params = NULL)
}
\arguments{
\item{sql}{Character string containing the SQL query to execute}

\item{project}{Project name (default: current project from options)}

\item{collect}{Logical; if TRUE (default), returns a data.frame. If FALSE,
returns a lazy dplyr table for further manipulation}

\item{params}{Optional named list of parameters for parameterized queries}
}
\value{
If collect=TRUE, returns a data.frame with query results.
  If collect=FALSE, returns a lazy dplyr tbl for further manipulation.
}
\description{
This function allows you to run arbitrary SQL queries against the tables
in your OmicsLake project. It provides full access to DuckDB's SQL
capabilities
including JOINs, aggregations, window functions, and more. Table names can be
referenced without the schema prefix (e.g., 'genes' instead of 'ol.genes').
}
\examples{
ol_init("ex_query", root = tempfile())
ol_write("t", data.frame(x = 1:3))
ol_query("SELECT COUNT(*) AS n FROM t")
}
