% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/observe.R
\name{.ol_track_validate_enable_args}
\alias{.ol_track_validate_enable_args}
\title{Enable transparent background tracking for a session}
\usage{
.ol_track_validate_enable_args(
  snapshot,
  store_observation,
  observation_name,
  auto_disable
)
}
\arguments{
\item{snapshot}{Optional snapshot label created when tracking is disabled
with commit}

\item{store_observation}{If TRUE, store observed reads/writes/lineage as an
object}

\item{observation_name}{Optional target name when `store_observation = TRUE`}

\item{auto_disable}{If TRUE, installs a `.Last()` hook to auto-commit on
session end}

\item{project}{Optional project name. If set, calls `use_lake(project, ...)`.}

\item{prefix}{Prefix for file-based node names in the lake}

\item{track_functions}{Character vector forwarded to tracking wrappers}

\item{observation_depends_on}{Dependencies used for observation record:
`"writes"`, `"reads"`, `"both"`, or `"none"`}

\item{...}{Additional arguments passed to `use_lake()` when `project` is
provided}
}
\value{
Invisible Lake object
}
\description{
Installs temporary wrappers for common unqualified I/O functions in
`.GlobalEnv`, so existing analysis code can run unchanged while reads/writes
are tracked and recorded to Lake.
}
\examples{
if (FALSE) {
    # In .Rprofile (one-time setup for transparent mode)
    OmicsLake::ol_enable_transparent_tracking(project = "rna_project")

    # Existing script code runs unchanged
    data <- read.csv("counts.csv")
    write.csv(data, "counts_qc.csv", row.names = FALSE)

    # Explicit end if not using auto_disable
    OmicsLake::ol_disable_transparent_tracking(commit = TRUE)
}
}
